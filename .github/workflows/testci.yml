name: Sample Run Include Syntax Json Format

on:
  push :
    paths:
        - '.github/workflows/testci.yml'
      
      
jobs:
#  job1:
#    runs-on: ubuntu-latest
#    outputs:
#       matrix: ${{ steps.setmatrix.outputs.matrix }}
#    steps:
#      - name: Set Dynamic Matrix
#        id: setmatrix
#        run: |
#          matrixStringifiedObject=' \
#          { \
#            "include": [ \
#              { \
#                "run": "run1" \
#              }, \
#              { \
#                "run": "run2" \
#              } \
#            ] \
#          } \
#          '
#
#          echo "::set-output name=matrix::$matrixStringifiedObject"
  job2:
#    needs: job1
    runs-on: ubuntu-latest
    strategy:
      matrix: 
        components:
          - name: a
            val: 1
          - name: b
            val: 2


    steps:
    - run: echo Run ${{ matrix.components.0.name }}
    - run: date
    - run: sleep 1
    - run: date
